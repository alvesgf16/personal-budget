import { Component } from '@angular/core';

/** Empty Dashboard tab. Feature UI lands in later tickets (PB-29). */
@Component({
  selector: 'app-dashboard',
  template: '',
})
export class Dashboard {}
