/**
 * Named piles (“collections”) in the document store.
 * Use these constants instead of raw strings so a typo fails at compile time.
 */
export const COLLECTIONS = {
  settings: 'settings',
  categories: 'categories',
} as const;
