---
description: Document store API — inject DocumentStoreService; no Dexie/SQL from feature code
globs: src/data/**
alwaysApply: false
---

# Document store (`src/data`)

- Feature screens inject `DocumentStoreService` — do not import Dexie, `BudgetDatabase`, or `createDocumentStore` from UI code.
- Collection names live in `COLLECTIONS` (`settings`, `categories`, …). Prefer those constants over raw strings.
- Domain payloads: parse with Zod schemas (`settingsSchema`, `categorySchema`) **before** `insert` / `update`. Store metadata (`id`, `updatedAt`, `deletedAt`) is added by the store, not by the schema.
- Every document has sync metadata: `id`, `updatedAt`, `deletedAt` (`null` while active).
- Soft-delete sets `deletedAt`; `list` hides tombstones; `getById` still returns them.
- Persistence is IndexedDB via Dexie — never query SQL from app code.
- Tests override the `DOCUMENT_STORE` token with `createDocumentStore(uniqueName)`.
