---
description: Core Personal Budget (PB) project constraints for every session
alwaysApply: true
---

# Personal Budget (PB)

- Jira project **PB**. Prefer **one story per session**.
- Stack: **Angular web, local-only** — no cloud API and no native shell in v1.
- Persistence is a **document store** (Dexie / IndexedDB via `DocumentStoreService`), not SQL.
- Commits: Conventional Commits; put the Jira key in the **body**, not the summary.
- **Feature work:** one branch per story, open a PR — do not push feature commits to `main`. Never `--no-verify`.
- Keep changes scoped; run `npm run lint` and `npm test` before claiming done.
