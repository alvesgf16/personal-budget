# Block direct pushes to main. Use a feature branch and open a PR.
while read local_ref local_sha remote_ref remote_sha; do
  if [ "$remote_ref" = "refs/heads/main" ]; then
    echo "Direct pushes to main are blocked. Use a feature branch and open a PR."
    exit 1
  fi
done

exit 0
